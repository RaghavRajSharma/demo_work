$("#add_remove_wishlist").html("<%=j render partial: 'wishlist_add_remove', locals: {listing_id: listing.id}%>");
$("#add_remove_wishlist_<%=listing.id%>").html("<%=j render partial: 'wishlist_add_remove', locals: {listing_id: listing.id}%>");
